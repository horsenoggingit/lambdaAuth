- need to add an authed page in the app that verifies that was actually have auth, and if not kicks back to the login/signup screen.
- also need to understand how to refresh credentials from the stored AWS token.
- work on iOS app with same setup as angular

- DONE: Roles for cognito are not handled correctly (need a way to update the policy with pool ID)
